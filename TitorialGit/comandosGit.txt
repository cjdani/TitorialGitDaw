git init
git config --global user.name "cjdani"
git config --global user.email "email"
git config --list
git branch -M main
git status
git add .
git commit -m "comentario"
git remote add identificador URL_do_repositorio_remoto_GitHub
git push -u identificador rama
